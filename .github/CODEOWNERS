# Code owners for SUM Chain.
#
# Advisory only: this exists so GitHub suggests a default reviewer on pull
# requests. It is NOT a gate. "Require review from Code Owners" is OFF, and
# branch protection asks for an ordinary human approval instead.
#
# The previous path-scoped version of this file was machinery for the #219
# automated-review gate — a compensating control for setting the approval count
# to zero. That gate has been removed (#239): repository CI and code review are
# development controls, not chain authority, and must not be built up as
# substitutes for on-chain validation.

* @sunhaoxiangwang
