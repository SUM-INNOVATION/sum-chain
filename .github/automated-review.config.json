{
  "_comment": "Config for the independent automated-review gate (#219). Edited only via an admin-overridden PR (it is a gate file). 'designated_checks' are the required CI/security checks that MUST all pass on the PR's current head before the automated review can pass. 'gate_paths' are the governance files whose modification requires an admin/maintainer approval on the current head (self-weakening refusal).",
  "designated_checks": [
    "build-test-clippy",
    "build-test-clippy-aarch64",
    "supply-chain-audit"
  ],
  "gate_paths": [
    ".github/workflows/automated-review.yml",
    ".github/workflows/approval-policy.yml",
    ".github/scripts/automated_review.py",
    ".github/scripts/automated_review_test.py",
    ".github/automated-review.config.json",
    "docs/governance/AUTOMATED-REVIEW.md"
  ]
}
