[package]
name = "b0-pre-candidate-sp1-host"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"
description = "SP1 6.3.1 host-side candidate pins (verifier + SDK + build). Resolved authoritatively only in the container venue."

# Exact direct pins (SP1 6.3.1). Never silently relaxed or replaced.
[dependencies]
sp1-sdk = "=6.3.1"
sp1-verifier = "=6.3.1"

[build-dependencies]
sp1-build = "=6.3.1"
