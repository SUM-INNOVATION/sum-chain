//! Placeholder crate root: exists only so the SP1 host-side pins resolve into a
//! single candidate Cargo.lock inside the container venue. No logic here; the
//! authoritative verifier-material extraction lives in
//! `harness/sp1-verifier-material`.
