//! Placeholder crate root so the SP1 guest-side pin (`sp1-zkvm`) resolves into
//! the single candidate Cargo.lock inside the container venue. No guest program
//! is defined here; this is NOT an official guest.
