[package]
name = "b0-pre-candidate-sp1-guest"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"
description = "SP1 6.3.1 guest-side candidate pin (zkVM runtime). Resolved authoritatively only in the container venue."

# Exact direct pin (SP1 6.3.1 guest runtime).
[dependencies]
sp1-zkvm = "=6.3.1"
