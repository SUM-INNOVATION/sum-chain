//! Placeholder crate root so the RISC Zero host-side pins resolve into a single
//! candidate Cargo.lock inside the container venue. The authoritative
//! verifier-material extraction lives in `harness/risc0-verifier-material`.
