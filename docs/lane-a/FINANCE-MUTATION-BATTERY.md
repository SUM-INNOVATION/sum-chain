# Finance migration: mutation battery manifest

Generated by `mut/run.py` against the worktree at commit-time. Every row
below is a mutation that was verified to resolve, applied, run against its
named covering test, and restored. The `test ... ok|FAILED` lines are the
ones libtest printed while that mutation was in the tree -- a summary count
cannot distinguish a test that failed from a test that never ran, and an
unresolved anchor looks exactly like a survivor in a total.

    49 mutations, 49 killed, 0 survived, anchors-not-found 0, covering-tests-not-run 0, does-not-compile 0, anchors-unrestored 0, residue none

A mutation is KILLED when its covering test appears in the output AND that
test failed. Anything else -- unresolved anchor, compile failure, a test
that did not run -- is reported as its own category and is NOT a kill.

## Summary by category

| category | mutations | killed |
|---|---|---|
| candidate-read-becomes-parent-read | 13 | 13 |
| codec-changed | 7 | 7 |
| decode-error-becomes-absence | 8 | 8 |
| dispatch-surface-short-circuited | 2 | 2 |
| key-builder-broken | 9 | 9 |
| multi-row-write-order-reversed | 1 | 1 |
| staged-family-omitted | 9 | 9 |

## Mutation -> anchor -> covering test

| # | id | category | file | anchor sha256/16 | covering test | outcome |
|---|---|---|---|---|---|---|
| 1 | `read/v_get_issuer` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `126ba50f9cfe104e` | `an_attestation_finds_an_issuer_registered_earlier_in_the_same_block` | KILLED |
| 2 | `read/v_get_address_proof` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `feb4d4a360bfbcdc` | `a_revocation_finds_the_address_proof_created_earlier_in_the_same_block` | KILLED |
| 3 | `read/v_get_bank_standing` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `b918974eb99825b4` | `a_bank_standing_update_and_revocation_find_the_same_block_creation` | KILLED |
| 4 | `read/v_get_jurisdiction_issuer_addresses` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `fb34882532d01470` | `two_issuers_in_one_jurisdiction_accumulate_in_the_jurisdiction_index` | KILLED |
| 5 | `read/v_get_subject_address_proof_ids` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `9fc31333ade5b5f5` | `two_address_proofs_for_one_subject_accumulate_in_the_address_index` | KILLED |
| 6 | `read/v_get_subject_bank_standing_ids` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `019c833efe449fc9` | `two_bank_standings_for_one_subject_accumulate_in_the_bank_index` | KILLED |
| 7 | `read/v_get_kyc_attestation` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `f40df647a6537566` | `a_kyc_update_and_revocation_find_the_same_block_creation` | KILLED |
| 8 | `read/v_get_subject_kyc_ids` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `434c082548d9212d` | `two_attestations_for_one_subject_accumulate_in_the_kyc_index` | KILLED |
| 9 | `read/v_issuer_exists` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `41850bdeaadc12ae` | `a_duplicate_issuer_registration_in_the_same_block_is_refused` | KILLED |
| 10 | `read/v_address_proof_exists` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `debf8dd4f48d3dd6` | `a_duplicate_address_proof_id_in_the_same_block_is_refused` | KILLED |
| 11 | `read/v_bank_standing_exists` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `06fae7022b1cb00a` | `a_duplicate_bank_standing_id_in_the_same_block_is_refused` | KILLED |
| 12 | `read/v_kyc_attestation_exists` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `0cbb15209d304135` | `a_duplicate_attestation_id_in_the_same_block_is_refused` | KILLED |
| 13 | `read/v_proof_exists` | candidate-read-becomes-parent-read | `crates/state/src/finance_view.rs` | `0bb66c9f02d870b3` | `a_duplicate_proof_id_in_the_same_block_is_refused` | KILLED |
| 14 | `omit/FINANCE_ISSUERS` | staged-family-omitted | `crates/state/src/finance_view.rs` | `12bfc271fd61ef7e` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 15 | `omit/FINANCE_JURISDICTION_INDEX` | staged-family-omitted | `crates/state/src/finance_view.rs` | `502d6c6c931c73cc` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 16 | `omit/FINANCE_ADDRESS_PROOFS` | staged-family-omitted | `crates/state/src/finance_view.rs` | `b5798cb7759018ba` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 17 | `omit/FINANCE_SUBJECT_ADDRESS_INDEX` | staged-family-omitted | `crates/state/src/finance_view.rs` | `751ce2235b8aa80c` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 18 | `omit/FINANCE_BANK_STANDINGS` | staged-family-omitted | `crates/state/src/finance_view.rs` | `7632f9e9ba80d800` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 19 | `omit/FINANCE_SUBJECT_BANK_INDEX` | staged-family-omitted | `crates/state/src/finance_view.rs` | `8db3fc84414cffb0` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 20 | `omit/FINANCE_KYC_ATTESTATIONS` | staged-family-omitted | `crates/state/src/finance_view.rs` | `fee091e7e9a37330` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 21 | `omit/FINANCE_SUBJECT_KYC_INDEX` | staged-family-omitted | `crates/state/src/finance_view.rs` | `71f222248f623dfd` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 22 | `omit/FINANCE_PROOFS` | staged-family-omitted | `crates/state/src/finance_view.rs` | `c89dd2c688c7e680` | `an_abandoned_block_leaves_all_nine_families_untouched` | KILLED |
| 23 | `key/issuer_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `baedb41f78fe3e26` | `an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction` | KILLED |
| 24 | `key/jurisdiction_index_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `24c3c831b1ab3c10` | `a_second_issuer_in_one_jurisdiction_appends_to_the_same_list` | KILLED |
| 25 | `key/address_proof_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `6a2db837975b3969` | `an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject` | KILLED |
| 26 | `key/subject_address_index_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `96334e01df3d296a` | `a_second_address_proof_for_one_subject_appends_to_the_same_list` | KILLED |
| 27 | `key/bank_standing_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `68143129246cbf0f` | `a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject` | KILLED |
| 28 | `key/subject_bank_index_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `8c5e67131211220d` | `a_second_bank_standing_for_one_subject_appends_to_the_same_list` | KILLED |
| 29 | `key/kyc_attestation_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `4f42e463a18f1ab3` | `a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject` | KILLED |
| 30 | `key/subject_kyc_index_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `02c2c69aae7e6b1f` | `a_second_kyc_attestation_for_one_subject_appends_to_the_same_list` | KILLED |
| 31 | `key/proof_key` | key-builder-broken | `crates/storage/src/finance_store.rs` | `5af8daf44557c6e1` | `a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing` | KILLED |
| 32 | `codec/encode_issuer` | codec-changed | `crates/storage/src/finance_store.rs` | `957a605080340571` | `an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction` | KILLED |
| 33 | `codec/encode_addresses` | codec-changed | `crates/storage/src/finance_store.rs` | `e663115ac28c4903` | `a_second_issuer_in_one_jurisdiction_appends_to_the_same_list` | KILLED |
| 34 | `codec/encode_address_proof` | codec-changed | `crates/storage/src/finance_store.rs` | `a44668518b515100` | `an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject` | KILLED |
| 35 | `codec/encode_bank_standing` | codec-changed | `crates/storage/src/finance_store.rs` | `0d1cb55d648ae67c` | `a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject` | KILLED |
| 36 | `codec/encode_kyc_attestation` | codec-changed | `crates/storage/src/finance_store.rs` | `5820d97b419b28a6` | `a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject` | KILLED |
| 37 | `codec/encode_proof` | codec-changed | `crates/storage/src/finance_store.rs` | `86b0c079e55d459a` | `a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing` | KILLED |
| 38 | `codec/encode_id_list` | codec-changed | `crates/storage/src/finance_store.rs` | `4bf86d04f0c028cd` | `a_second_address_proof_for_one_subject_appends_to_the_same_list` | KILLED |
| 39 | `decode-absence/v_get_issuer` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `6dd5bd164addf217` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 40 | `decode-absence/v_get_jurisdiction_issuer_addresses` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `aff32bdb84a1e57a` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 41 | `decode-absence/v_get_address_proof` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `957f2ba9c0c3de73` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 42 | `decode-absence/v_get_subject_address_proof_ids` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `0e456da18d3accff` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 43 | `decode-absence/v_get_bank_standing` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `47662e0859abc74f` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 44 | `decode-absence/v_get_subject_bank_standing_ids` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `2dc8423f27e272df` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 45 | `decode-absence/v_get_kyc_attestation` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `10764546d830dfd7` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 46 | `decode-absence/v_get_subject_kyc_ids` | decode-error-becomes-absence | `crates/state/src/finance_view.rs` | `360037670ca69d8f` | `malformed_rows_error_through_dispatch_and_commit_nothing` | KILLED |
| 47 | `order/v_put_kyc_attestation` | multi-row-write-order-reversed | `crates/state/src/finance_view.rs` | `60143ee7ebfe584c` | `a_refusal_leaves_canonical_storage_untouched` | KILLED |
| 48 | `dispatch/execute_tx_with_validators` | dispatch-surface-short-circuited | `crates/state/src/executor.rs` | `07665e83b6f799cb` | `without_the_registration_the_same_attestation_is_refused` | KILLED |
| 49 | `dispatch/execute_tx_v2` | dispatch-surface-short-circuited | `crates/state/src/executor.rs` | `cdab9142c18645b2` | `the_v2_dispatch_surface_refuses_a_duplicate_with_the_finance_status` | KILLED |

## Verbatim record

### 1. `read/v_get_issuer`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `126ba50f9cfe104e`
* replacement sha256/16: `dcf87d8ce9246a8c`
* covering test: `an_attestation_finds_an_issuer_registered_earlier_in_the_same_block` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let issuer_row = view
            .get(cf::FINANCE_ISSUERS, issuer_key(issuer_address))
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let issuer_row = match view.preimage(cf::FINANCE_ISSUERS, issuer_key(issuer_address)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_ISSUERS, issuer_key(issuer_address))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test an_attestation_finds_an_issuer_registered_earlier_in_the_same_block ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.15s
```

### 2. `read/v_get_address_proof`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `feb4d4a360bfbcdc`
* replacement sha256/16: `86546a3a57557469`
* covering test: `a_revocation_finds_the_address_proof_created_earlier_in_the_same_block` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let address_proof_row = view
            .get(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id))
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let address_proof_row = match view.preimage(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test a_revocation_finds_the_address_proof_created_earlier_in_the_same_block ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.19s
```

### 3. `read/v_get_bank_standing`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `b918974eb99825b4`
* replacement sha256/16: `4e8f2714ed648097`
* covering test: `a_bank_standing_update_and_revocation_find_the_same_block_creation` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let bank_standing_row = view
            .get(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id))
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let bank_standing_row = match view.preimage(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test a_bank_standing_update_and_revocation_find_the_same_block_creation ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.19s
```

### 4. `read/v_get_jurisdiction_issuer_addresses`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `fb34882532d01470`
* replacement sha256/16: `7486446d3f5a2650`
* covering test: `two_issuers_in_one_jurisdiction_accumulate_in_the_jurisdiction_index` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let jurisdiction_row = view
            .get(
                cf::FINANCE_JURISDICTION_INDEX,
                jurisdiction_index_key(jurisdiction_code),
            )
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let jurisdiction_row = match view.preimage(cf::FINANCE_JURISDICTION_INDEX, jurisdiction_index_key(jurisdiction_code)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_JURISDICTION_INDEX, jurisdiction_index_key(jurisdiction_code))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test two_issuers_in_one_jurisdiction_accumulate_in_the_jurisdiction_index ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.19s
```

### 5. `read/v_get_subject_address_proof_ids`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `9fc31333ade5b5f5`
* replacement sha256/16: `4b07de750e02cdeb`
* covering test: `two_address_proofs_for_one_subject_accumulate_in_the_address_index` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let subject_address_row = view
            .get(
                cf::FINANCE_SUBJECT_ADDRESS_INDEX,
                subject_address_index_key(subject_ref),
            )
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let subject_address_row = match view.preimage(cf::FINANCE_SUBJECT_ADDRESS_INDEX, subject_address_index_key(subject_ref)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_SUBJECT_ADDRESS_INDEX, subject_address_index_key(subject_ref))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test two_address_proofs_for_one_subject_accumulate_in_the_address_index ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.18s
```

### 6. `read/v_get_subject_bank_standing_ids`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `019c833efe449fc9`
* replacement sha256/16: `d308d35aee8a94de`
* covering test: `two_bank_standings_for_one_subject_accumulate_in_the_bank_index` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let subject_bank_row = view
            .get(
                cf::FINANCE_SUBJECT_BANK_INDEX,
                subject_bank_index_key(subject_ref),
            )
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let subject_bank_row = match view.preimage(cf::FINANCE_SUBJECT_BANK_INDEX, subject_bank_index_key(subject_ref)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_SUBJECT_BANK_INDEX, subject_bank_index_key(subject_ref))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test two_bank_standings_for_one_subject_accumulate_in_the_bank_index ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.21s
```

### 7. `read/v_get_kyc_attestation`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `f40df647a6537566`
* replacement sha256/16: `68a6b9415869c808`
* covering test: `a_kyc_update_and_revocation_find_the_same_block_creation` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let kyc_attestation_row = view
            .get(
                cf::FINANCE_KYC_ATTESTATIONS,
                kyc_attestation_key(attestation_id),
            )
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let kyc_attestation_row = match view.preimage(cf::FINANCE_KYC_ATTESTATIONS, kyc_attestation_key(attestation_id)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_KYC_ATTESTATIONS, kyc_attestation_key(attestation_id))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test a_kyc_update_and_revocation_find_the_same_block_creation ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.18s
```

### 8. `read/v_get_subject_kyc_ids`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `434c082548d9212d`
* replacement sha256/16: `6703ffceb39c2f03`
* covering test: `two_attestations_for_one_subject_accumulate_in_the_kyc_index` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        let subject_kyc_row = view
            .get(
                cf::FINANCE_SUBJECT_KYC_INDEX,
                subject_kyc_index_key(subject_ref),
            )
            .map_err(StateError::Storage)?;
```

replacement:

```rust
        let subject_kyc_row = match view.preimage(cf::FINANCE_SUBJECT_KYC_INDEX, subject_kyc_index_key(subject_ref)) {
            Some(before) => before.clone(),
            None => view
                .get(cf::FINANCE_SUBJECT_KYC_INDEX, subject_kyc_index_key(subject_ref))
                .map_err(StateError::Storage)?,
        };
```

covering-test output while mutated:

```
test two_attestations_for_one_subject_accumulate_in_the_kyc_index ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.19s
```

### 9. `read/v_issuer_exists`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `41850bdeaadc12ae`
* replacement sha256/16: `446496d6781b5ccd`
* covering test: `a_duplicate_issuer_registration_in_the_same_block_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.contains(cf::FINANCE_ISSUERS, issuer_key(issuer_address))
            .map_err(StateError::Storage)
    }
```

replacement:

```rust
        match view.preimage(cf::FINANCE_ISSUERS, issuer_key(issuer_address)) {
            Some(before) => Ok(before.is_some()),
            None => view
                .contains(cf::FINANCE_ISSUERS, issuer_key(issuer_address))
                .map_err(StateError::Storage),
        }
    }
```

covering-test output while mutated:

```
test a_duplicate_issuer_registration_in_the_same_block_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 10. `read/v_address_proof_exists`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `debf8dd4f48d3dd6`
* replacement sha256/16: `97cc160fdf7ef486`
* covering test: `a_duplicate_address_proof_id_in_the_same_block_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.contains(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id))
            .map_err(StateError::Storage)
    }
```

replacement:

```rust
        match view.preimage(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id)) {
            Some(before) => Ok(before.is_some()),
            None => view
                .contains(cf::FINANCE_ADDRESS_PROOFS, address_proof_key(proof_id))
                .map_err(StateError::Storage),
        }
    }
```

covering-test output while mutated:

```
test a_duplicate_address_proof_id_in_the_same_block_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 11. `read/v_bank_standing_exists`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `06fae7022b1cb00a`
* replacement sha256/16: `dfea7ecad9a6c087`
* covering test: `a_duplicate_bank_standing_id_in_the_same_block_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.contains(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id))
            .map_err(StateError::Storage)
    }
```

replacement:

```rust
        match view.preimage(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id)) {
            Some(before) => Ok(before.is_some()),
            None => view
                .contains(cf::FINANCE_BANK_STANDINGS, bank_standing_key(credential_id))
                .map_err(StateError::Storage),
        }
    }
```

covering-test output while mutated:

```
test a_duplicate_bank_standing_id_in_the_same_block_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 12. `read/v_kyc_attestation_exists`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `0cbb15209d304135`
* replacement sha256/16: `9a7ca278d49123d7`
* covering test: `a_duplicate_attestation_id_in_the_same_block_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.contains(
            cf::FINANCE_KYC_ATTESTATIONS,
            kyc_attestation_key(attestation_id),
        )
        .map_err(StateError::Storage)
    }
```

replacement:

```rust
        match view.preimage(
            cf::FINANCE_KYC_ATTESTATIONS,
            kyc_attestation_key(attestation_id),
        ) {
            Some(before) => Ok(before.is_some()),
            None => view
                .contains(
                    cf::FINANCE_KYC_ATTESTATIONS,
                    kyc_attestation_key(attestation_id),
                )
                .map_err(StateError::Storage),
        }
    }
```

covering-test output while mutated:

```
test a_duplicate_attestation_id_in_the_same_block_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.17s
```

### 13. `read/v_proof_exists`

* category: candidate-read-becomes-parent-read
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `0bb66c9f02d870b3`
* replacement sha256/16: `a4ed98a625e67876`
* covering test: `a_duplicate_proof_id_in_the_same_block_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.contains(cf::FINANCE_PROOFS, proof_key(proof_id))
            .map_err(StateError::Storage)
    }
```

replacement:

```rust
        match view.preimage(cf::FINANCE_PROOFS, proof_key(proof_id)) {
            Some(before) => Ok(before.is_some()),
            None => view
                .contains(cf::FINANCE_PROOFS, proof_key(proof_id))
                .map_err(StateError::Storage),
        }
    }
```

covering-test output while mutated:

```
test a_duplicate_proof_id_in_the_same_block_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 14. `omit/FINANCE_ISSUERS`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `12bfc271fd61ef7e`
* replacement sha256/16: `947791dfc172fc12`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_ISSUERS,
            issuer_key(&issuer.issuer_address),
            &issuer_bytes,
        )
        .map_err(StateError::Storage)?;
```

replacement:

```rust
        let _ = &issuer_bytes;
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 15. `omit/FINANCE_JURISDICTION_INDEX`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `502d6c6c931c73cc`
* replacement sha256/16: `700af44e9fce671c`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_JURISDICTION_INDEX,
            jurisdiction_index_key(jurisdiction_code),
            &index_bytes,
        )
        .map_err(StateError::Storage)
    }
```

replacement:

```rust
        let _ = &index_bytes;
        Ok(())
    }
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 16. `omit/FINANCE_ADDRESS_PROOFS`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `b5798cb7759018ba`
* replacement sha256/16: `1d581b2f183aae77`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_ADDRESS_PROOFS,
            address_proof_key(&proof.proof_id),
            &address_proof_bytes,
        )
        .map_err(StateError::Storage)?;
```

replacement:

```rust
        let _ = &address_proof_bytes;
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 17. `omit/FINANCE_SUBJECT_ADDRESS_INDEX`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `751ce2235b8aa80c`
* replacement sha256/16: `c6c118c3b8415d2a`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_SUBJECT_ADDRESS_INDEX,
            subject_address_index_key(subject_ref),
            &subject_address_bytes,
        )
        .map_err(StateError::Storage)
    }
```

replacement:

```rust
        let _ = &subject_address_bytes;
        Ok(())
    }
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 18. `omit/FINANCE_BANK_STANDINGS`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `7632f9e9ba80d800`
* replacement sha256/16: `e87880d2e709bfe6`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_BANK_STANDINGS,
            bank_standing_key(&credential.credential_id),
            &bank_standing_bytes,
        )
        .map_err(StateError::Storage)?;
```

replacement:

```rust
        let _ = &bank_standing_bytes;
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 19. `omit/FINANCE_SUBJECT_BANK_INDEX`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `8db3fc84414cffb0`
* replacement sha256/16: `58b72763b048d13a`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_SUBJECT_BANK_INDEX,
            subject_bank_index_key(subject_ref),
            &subject_bank_bytes,
        )
        .map_err(StateError::Storage)
    }
```

replacement:

```rust
        let _ = &subject_bank_bytes;
        Ok(())
    }
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 20. `omit/FINANCE_KYC_ATTESTATIONS`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `fee091e7e9a37330`
* replacement sha256/16: `d990e8800951011a`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_KYC_ATTESTATIONS,
            kyc_attestation_key(&attestation.attestation_id),
            &kyc_attestation_bytes,
        )
        .map_err(StateError::Storage)?;
```

replacement:

```rust
        let _ = &kyc_attestation_bytes;
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 21. `omit/FINANCE_SUBJECT_KYC_INDEX`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `71f222248f623dfd`
* replacement sha256/16: `a5471af7704d0e19`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_SUBJECT_KYC_INDEX,
            subject_kyc_index_key(subject_ref),
            &subject_kyc_bytes,
        )
        .map_err(StateError::Storage)
    }
```

replacement:

```rust
        let _ = &subject_kyc_bytes;
        Ok(())
    }
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 22. `omit/FINANCE_PROOFS`

* category: staged-family-omitted
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `c89dd2c688c7e680`
* replacement sha256/16: `52f78ade0770b943`
* covering test: `an_abandoned_block_leaves_all_nine_families_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(cf::FINANCE_PROOFS, proof_key(&proof.proof_id), &proof_bytes)
            .map_err(StateError::Storage)
    }
```

replacement:

```rust
        let _ = &proof_bytes;
        Ok(())
    }
```

covering-test output while mutated:

```
test an_abandoned_block_leaves_all_nine_families_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 23. `key/issuer_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `baedb41f78fe3e26`
* replacement sha256/16: `361d84944b895576`
* covering test: `an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn issuer_key(issuer_address: &Address) -> &[u8] {
    issuer_address.as_bytes()
}
```

replacement:

```rust
pub fn issuer_key(issuer_address: &Address) -> &[u8] {
    &issuer_address.as_bytes()[1..]
}
```

covering-test output while mutated:

```
test an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 24. `key/jurisdiction_index_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `24c3c831b1ab3c10`
* replacement sha256/16: `fdd638a0eb6460ea`
* covering test: `a_second_issuer_in_one_jurisdiction_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn jurisdiction_index_key(jurisdiction_code: &str) -> &[u8] {
    jurisdiction_code.as_bytes()
}
```

replacement:

```rust
pub fn jurisdiction_index_key(jurisdiction_code: &str) -> &[u8] {
    &jurisdiction_code.as_bytes()[..jurisdiction_code.len().saturating_sub(1)]
}
```

covering-test output while mutated:

```
test a_second_issuer_in_one_jurisdiction_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 25. `key/address_proof_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `6a2db837975b3969`
* replacement sha256/16: `c11cb97ad8d0a26e`
* covering test: `an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn address_proof_key(proof_id: &AddressProofId) -> &[u8] {
    proof_id
}
```

replacement:

```rust
pub fn address_proof_key(proof_id: &AddressProofId) -> &[u8] {
    &proof_id[1..]
}
```

covering-test output while mutated:

```
test an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 26. `key/subject_address_index_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `96334e01df3d296a`
* replacement sha256/16: `8cb3a3c6fabdd695`
* covering test: `a_second_address_proof_for_one_subject_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn subject_address_index_key(subject_ref: &SubjectRef) -> &[u8] {
    subject_ref
}
```

replacement:

```rust
pub fn subject_address_index_key(subject_ref: &SubjectRef) -> &[u8] {
    &subject_ref[1..]
}
```

covering-test output while mutated:

```
test a_second_address_proof_for_one_subject_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 27. `key/bank_standing_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `68143129246cbf0f`
* replacement sha256/16: `8049c510aea62a33`
* covering test: `a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn bank_standing_key(credential_id: &BankStandingId) -> &[u8] {
    credential_id
}
```

replacement:

```rust
pub fn bank_standing_key(credential_id: &BankStandingId) -> &[u8] {
    &credential_id[1..]
}
```

covering-test output while mutated:

```
test a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 28. `key/subject_bank_index_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `8c5e67131211220d`
* replacement sha256/16: `9bb62137f3c5944c`
* covering test: `a_second_bank_standing_for_one_subject_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn subject_bank_index_key(subject_ref: &SubjectRef) -> &[u8] {
    subject_ref
}
```

replacement:

```rust
pub fn subject_bank_index_key(subject_ref: &SubjectRef) -> &[u8] {
    &subject_ref[1..]
}
```

covering-test output while mutated:

```
test a_second_bank_standing_for_one_subject_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 29. `key/kyc_attestation_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `4f42e463a18f1ab3`
* replacement sha256/16: `58efa0cf23d57bfb`
* covering test: `a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn kyc_attestation_key(attestation_id: &KycAttestationId) -> &[u8] {
    attestation_id
}
```

replacement:

```rust
pub fn kyc_attestation_key(attestation_id: &KycAttestationId) -> &[u8] {
    &attestation_id[1..]
}
```

covering-test output while mutated:

```
test a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 30. `key/subject_kyc_index_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `02c2c69aae7e6b1f`
* replacement sha256/16: `260cc4f2a1167cb6`
* covering test: `a_second_kyc_attestation_for_one_subject_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn subject_kyc_index_key(subject_ref: &SubjectRef) -> &[u8] {
    subject_ref
}
```

replacement:

```rust
pub fn subject_kyc_index_key(subject_ref: &SubjectRef) -> &[u8] {
    &subject_ref[1..]
}
```

covering-test output while mutated:

```
test a_second_kyc_attestation_for_one_subject_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 31. `key/proof_key`

* category: key-builder-broken
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `5af8daf44557c6e1`
* replacement sha256/16: `80074b18b4fdce4c`
* covering test: `a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn proof_key(proof_id: &ProofId) -> &[u8] {
    proof_id
}
```

replacement:

```rust
pub fn proof_key(proof_id: &ProofId) -> &[u8] {
    &proof_id[1..]
}
```

covering-test output while mutated:

```
test a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 32. `codec/encode_issuer`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `957a605080340571`
* replacement sha256/16: `3db9c86072ad2a30`
* covering test: `an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_issuer(issuer: &FinanceIssuerProfile) -> Result<Vec<u8>> {
    bincode::serialize(issuer).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_issuer(issuer: &FinanceIssuerProfile) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(issuer).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test an_issuer_row_is_bincode_at_the_address_key_and_indexes_its_jurisdiction ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 33. `codec/encode_addresses`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `e663115ac28c4903`
* replacement sha256/16: `2eb1bc8e5c38f9fb`
* covering test: `a_second_issuer_in_one_jurisdiction_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_addresses(addresses: &[Address]) -> Result<Vec<u8>> {
    bincode::serialize(addresses).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_addresses(addresses: &[Address]) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(addresses).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test a_second_issuer_in_one_jurisdiction_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 34. `codec/encode_address_proof`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `a44668518b515100`
* replacement sha256/16: `bddcdc1bacd74df9`
* covering test: `an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_address_proof(proof: &AddressProof) -> Result<Vec<u8>> {
    bincode::serialize(proof).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_address_proof(proof: &AddressProof) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(proof).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test an_address_proof_row_is_bincode_at_the_proof_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 35. `codec/encode_bank_standing`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `0d1cb55d648ae67c`
* replacement sha256/16: `0343d64075aa9453`
* covering test: `a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_bank_standing(credential: &BankStandingCredential) -> Result<Vec<u8>> {
    bincode::serialize(credential).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_bank_standing(credential: &BankStandingCredential) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(credential).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test a_bank_standing_row_is_bincode_at_the_credential_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 36. `codec/encode_kyc_attestation`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `5820d97b419b28a6`
* replacement sha256/16: `2b50726a8ce9ac28`
* covering test: `a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_kyc_attestation(attestation: &KycAttestation) -> Result<Vec<u8>> {
    bincode::serialize(attestation).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_kyc_attestation(attestation: &KycAttestation) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(attestation).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test a_kyc_row_is_bincode_at_the_attestation_id_and_indexes_its_subject ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 37. `codec/encode_proof`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `86b0c079e55d459a`
* replacement sha256/16: `c6256bb841f93fc3`
* covering test: `a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_proof(proof: &FinanceProofEnvelope) -> Result<Vec<u8>> {
    bincode::serialize(proof).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_proof(proof: &FinanceProofEnvelope) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(proof).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test a_proof_row_is_bincode_at_the_proof_id_key_and_indexes_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.13s
```

### 38. `codec/encode_id_list`

* category: codec-changed
* file: `crates/storage/src/finance_store.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `4bf86d04f0c028cd`
* replacement sha256/16: `e7ac55863218d6e9`
* covering test: `a_second_address_proof_for_one_subject_appends_to_the_same_list` (sumchain-storage, target `finance_codec_parity`)
* outcome: **KILLED**

anchor:

```rust
pub fn encode_id_list(ids: &[[u8; 32]]) -> Result<Vec<u8>> {
    bincode::serialize(ids).map_err(|e| StorageError::Serialization(e.to_string()))
}
```

replacement:

```rust
pub fn encode_id_list(ids: &[[u8; 32]]) -> Result<Vec<u8>> {
    let mut bytes =
        bincode::serialize(ids).map_err(|e| StorageError::Serialization(e.to_string()))?;
    bytes.push(0);
    Ok(bytes)
}
```

covering-test output while mutated:

```
test a_second_address_proof_for_one_subject_appends_to_the_same_list ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out; finished in 0.14s
```

### 39. `decode-absence/v_get_issuer`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `6dd5bd164addf217`
* replacement sha256/16: `c2b21a8c8a41f4b0`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
            Some(bytes) => Ok(Some(decode_issuer(&bytes).map_err(StateError::Storage)?)),
```

replacement:

```rust
            Some(bytes) => Ok(decode_issuer(&bytes).ok()),
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 40. `decode-absence/v_get_jurisdiction_issuer_addresses`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `aff32bdb84a1e57a`
* replacement sha256/16: `989cce07256c9dc0`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        match jurisdiction_row {
            Some(bytes) => decode_addresses(&bytes).map_err(StateError::Storage),
            None => Ok(Vec::new()),
        }
```

replacement:

```rust
        match jurisdiction_row {
            Some(bytes) => Ok(decode_addresses(&bytes).unwrap_or_default()),
            None => Ok(Vec::new()),
        }
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.24s
```

### 41. `decode-absence/v_get_address_proof`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `957f2ba9c0c3de73`
* replacement sha256/16: `5c4dd193660e98e7`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
            Some(bytes) => Ok(Some(
                decode_address_proof(&bytes).map_err(StateError::Storage)?,
            )),
```

replacement:

```rust
            Some(bytes) => Ok(decode_address_proof(&bytes).ok()),
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.36s
```

### 42. `decode-absence/v_get_subject_address_proof_ids`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `0e456da18d3accff`
* replacement sha256/16: `46539bfc7327b4d7`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        match subject_address_row {
            Some(bytes) => decode_id_list(&bytes).map_err(StateError::Storage),
            None => Ok(Vec::new()),
        }
```

replacement:

```rust
        match subject_address_row {
            Some(bytes) => Ok(decode_id_list(&bytes).unwrap_or_default()),
            None => Ok(Vec::new()),
        }
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.47s
```

### 43. `decode-absence/v_get_bank_standing`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `47662e0859abc74f`
* replacement sha256/16: `c1d72f32681a38a0`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
            Some(bytes) => Ok(Some(
                decode_bank_standing(&bytes).map_err(StateError::Storage)?,
            )),
```

replacement:

```rust
            Some(bytes) => Ok(decode_bank_standing(&bytes).ok()),
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.57s
```

### 44. `decode-absence/v_get_subject_bank_standing_ids`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `2dc8423f27e272df`
* replacement sha256/16: `ab77dc3edb723684`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        match subject_bank_row {
            Some(bytes) => decode_id_list(&bytes).map_err(StateError::Storage),
            None => Ok(Vec::new()),
        }
```

replacement:

```rust
        match subject_bank_row {
            Some(bytes) => Ok(decode_id_list(&bytes).unwrap_or_default()),
            None => Ok(Vec::new()),
        }
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.69s
```

### 45. `decode-absence/v_get_kyc_attestation`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `10764546d830dfd7`
* replacement sha256/16: `6f0d0fc586444579`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
            Some(bytes) => Ok(Some(
                decode_kyc_attestation(&bytes).map_err(StateError::Storage)?,
            )),
```

replacement:

```rust
            Some(bytes) => Ok(decode_kyc_attestation(&bytes).ok()),
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.79s
```

### 46. `decode-absence/v_get_subject_kyc_ids`

* category: decode-error-becomes-absence
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `360037670ca69d8f`
* replacement sha256/16: `67323c06cb05183a`
* covering test: `malformed_rows_error_through_dispatch_and_commit_nothing` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        match subject_kyc_row {
            Some(bytes) => decode_id_list(&bytes).map_err(StateError::Storage),
            None => Ok(Vec::new()),
        }
```

replacement:

```rust
        match subject_kyc_row {
            Some(bytes) => Ok(decode_id_list(&bytes).unwrap_or_default()),
            None => Ok(Vec::new()),
        }
```

covering-test output while mutated:

```
test malformed_rows_error_through_dispatch_and_commit_nothing ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.91s
```

### 47. `order/v_put_kyc_attestation`

* category: multi-row-write-order-reversed
* file: `crates/state/src/finance_view.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `60143ee7ebfe584c`
* replacement sha256/16: `35a3a5f1fa6067db`
* covering test: `a_refusal_leaves_canonical_storage_untouched` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
        view.put(
            cf::FINANCE_KYC_ATTESTATIONS,
            kyc_attestation_key(&attestation.attestation_id),
            &kyc_attestation_bytes,
        )
        .map_err(StateError::Storage)?;
        Self::v_add_to_subject_kyc_index(
            view,
            &attestation.subject_ref,
            &attestation.attestation_id,
        )
    }
```

replacement:

```rust
        Self::v_add_to_subject_kyc_index(
            view,
            &attestation.subject_ref,
            &attestation.attestation_id,
        )?;
        view.put(
            cf::FINANCE_KYC_ATTESTATIONS,
            kyc_attestation_key(&attestation.attestation_id),
            &kyc_attestation_bytes,
        )
        .map_err(StateError::Storage)
    }
```

covering-test output while mutated:

```
test a_refusal_leaves_canonical_storage_untouched ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.20s
```

### 48. `dispatch/execute_tx_with_validators`

* category: dispatch-surface-short-circuited
* file: `crates/state/src/executor.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `07665e83b6f799cb`
* replacement sha256/16: `16206eedb409c792`
* covering test: `without_the_registration_the_same_attestation_is_refused` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
                        if result.success {
                            debug!(
                                "V2 Finance {} executed: {:?}",
```

replacement:

```rust
                        if result.success || true {
                            debug!(
                                "V2 Finance {} executed: {:?}",
```

covering-test output while mutated:

```
test without_the_registration_the_same_attestation_is_refused ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```

### 49. `dispatch/execute_tx_v2`

* category: dispatch-surface-short-circuited
* file: `crates/state/src/executor.rs`
* anchor occurrences before applying: 1
* anchor sha256/16: `cdab9142c18645b2`
* replacement sha256/16: `3bc27daf73e7bc7d`
* covering test: `the_v2_dispatch_surface_refuses_a_duplicate_with_the_finance_status` (sumchain-state, target `finance_routing`)
* outcome: **KILLED**

anchor:

```rust
                if result.success {
                    debug!(
                        "V2 Finance {} executed: {:?}",
```

replacement:

```rust
                if result.success || true {
                    debug!(
                        "V2 Finance {} executed: {:?}",
```

covering-test output while mutated:

```
test the_v2_dispatch_surface_refuses_a_duplicate_with_the_finance_status ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 39 filtered out; finished in 0.13s
```
